export const collisionInit = ()=>{
    // let c = document.getElementById("frame");
    // let context = c.getContext("2d")
    // context.beginPath()
    // context.fillStyle = "red"
    // context.fillRect(pInfo.posX,pInfo.posY,pInfo.sizeH,pInfo.sizeW)
    // context.closePath()
}